to build HTML Indonesian holidays calendar:

- install `uv` dependency manager.

- run `uv sync`.

- run `uv run build.py`

public HTML Indonesian holidays calendar can be accessed through https://guangrei.github.io/APIHariLibur_V2/
